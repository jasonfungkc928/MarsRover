<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.GroupAssignment.marsrover">

    <application
        android:allowBackup="true"
        android:icon="@drawable/mars"
        android:label="@string/app_name"
        android:roundIcon="@drawable/mars"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ProfileScreen"></activity>
        <activity android:name=".Controller.MainActivity"></activity>
        <activity android:name=".HomeScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".JavaActivity"></activity>
        <activity android:name=".ShowLesson"></activity>
        <activity android:name=".Controller.QuestionActivity"/>
    </application>

</manifest>